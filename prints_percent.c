#include "main.h"
/**
 * prints_percent - prints percentage
 * @forms: argument
 * Return: the amount of characters printed
 */
int prints_percent(__attribute__((unused))va_list forms)
{
	_putchars('%');
	return (1);
}
