#include "main.h"

/**
 * _putchars - Evaluates if a character is printable
 * @c: character to be printed
 *
 * Return: 1 if c is printable, -1 otherwise
 */
int _putchars(char c)
{       
        return (write(1, &c< 1));
}
